# 📝 Emotional Notes

**Emotional Notes** — это мобильное приложение на Flutter для ведения личного дневника, которое помогает не только записывать мысли, но и отслеживать своё эмоциональное состояние. Каждая заметка ассоциируется с определенной эмоцией, что позволяет в дальнейшем анализировать ваше настроение с помощью статистики.

## ✨ Основные возможности

* **✍️ Ведение дневника**: Создание, редактирование и хранение текстовых заметок.
* **🎨 Эмоциональная окраска**: Привязка эмоции (цветового тега) к каждой записи для визуализации настроения.
* **📊 Статистика**: Экран с графиками, позволяющий увидеть соотношение различных эмоций за определенный период.
* **💾 Локальная база данных**: Все данные надежно хранятся на устройстве с использованием SQLite (через Drift), что обеспечивает быструю работу и конфиденциальность.
* **🔍 Фильтрация и поиск**: Удобный просмотр списка заметок.

## 🛠 Технологический стек

* **Фреймворк**: [Flutter](https://flutter.dev/) (Dart)
* **Управление состоянием**: [Riverpod](https://riverpod.dev/) (`ConsumerWidget`, `StateNotifier`) — для реактивного связывания UI и бизнес-логики.
* **База данных**: [Drift](https://drift.simonbinder.eu/) — мощная ORM для SQLite, обеспечивающая типобезопасные запросы.
* **Архитектура**: Feature-first (разделение по фичам: `feature/emotion_notes`), с выделением слоев:
    * **Data**: `database`, `tables`
    * **Domain/Model**: `repositories`
    * **Presentation**: `view` (Screens, Widgets), `viewmodel` (Notifiers).

## 📂 Структура проекта

Проект организован по принципам чистой архитектуры и масштабируемости:

```text
lib/
├── core/
│   ├── database/         # Конфигурация Drift (AppDatabase)
│   ├── utils/            # Утилиты (цвета эмоций)
│   └── dependencies/     # Модели объединения данных (NoteWithEmotions)
├── feature/
│   └── emotion_notes/    # Основная фича заметок
│       ├── database/     # Описание таблиц (Notes, Emotions)
│       ├── models/       # Репозитории
│       ├── view/         # UI: Экраны и Виджеты
│       │   ├── screens/  # NotesList, CreateEdit, Statistics
│       │   └── widgets/  # NoteListItem
│       └── viewmodel/    # Логика (Riverpod Providers & Notifiers)
└── main.dart             # Точка входа
